<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:gravity="center"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.timur.mainmenu.activity.MainActivity"
    tools:showIn="@layout/app_bar_main">

    <TextView
        android:id="@+id/trainingBeginText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Today's training is ready"
        android:textSize="25sp"
        android:gravity="center"
        android:layout_centerHorizontal="true"
        android:paddingBottom="20sp"
        android:textColor="@color/orange"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"/>
    <TextView
        android:id="@+id/motivationText"
        android:layout_width="250sp"
        android:layout_height="wrap_content"
        android:text="Perform better today, try to improve your previos result"
        android:textSize="15sp"
        android:textColor="@color/grey"
        android:paddingBottom="30sp"
        android:layout_below="@+id/trainingBeginText"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"/>
    <Button
        android:id="@+id/trainingBeginButton"
        android:layout_width="280sp"
        android:layout_height="wrap_content"
        android:text="begin"
        android:textSize="25sp"
        android:layout_below="@+id/motivationText"
        android:layout_centerHorizontal="true"
        android:background="@drawable/button_selector"
        android:textColor="@color/white"/>
</RelativeLayout>
